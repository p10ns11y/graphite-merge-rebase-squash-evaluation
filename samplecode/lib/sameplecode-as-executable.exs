defmodule SamplecodeScript do
  def main() do
    IO.puts("How are you?")
  end
end

SamplecodeScript.main()

# In terminal, run the following command to execute the script:
# elixir samplecode-as-executable.exs
